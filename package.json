{
  "name": "tourism-booking-engine",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist .parcel-cache",
    "start": "parcel serve js/app.js --dist-dir dist --port 3004",
    "build": "parcel build js/app.js --dist-dir dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "flatpickr": "^4.6.13"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "parcel-reporter-static-files-copy": "^1.5.3"
  }
}
